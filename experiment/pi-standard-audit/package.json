{
  "name": "pi-standard-audit",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Pi-based vertical agent for chunk-studio standard-value audit (capability probe)",
  "scripts": {
    "run": "node --env-file=.env --import tsx runner.ts",
    "run:case": "node --env-file=.env --import tsx runner.ts --case"
  },
  "dependencies": {
    "@earendil-works/pi-coding-agent": "0.85.1",
    "typebox": "^1.3.7",
    "tsx": "^4.19.0"
  }
}